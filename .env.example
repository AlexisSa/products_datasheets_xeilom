# URL de base pour les pages produits (lien "Voir le produit").
# Exemple: https://www.monsite.com
# Si vide, utilise les URLs du CSV (xeilom.fr).
VITE_PRODUCT_BASE_URL=https://www.xeilom.fr

# URL de base de l'API (pour le proxy de téléchargement).
# En production sur Vercel, laisser vide (même origine).
# En dev local avec "vercel dev", laisser vide.
VITE_API_BASE_URL=
